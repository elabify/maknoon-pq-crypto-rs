// Root project: declares plugin versions. Config lives in
// library/build.gradle.kts. Mirrors the ledger-*-rs android builds.

plugins {
    id("com.android.library") version "8.7.3" apply false
    id("org.jetbrains.kotlin.android") version "2.0.21" apply false
}
